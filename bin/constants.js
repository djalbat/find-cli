"use strict";

const FIND = "find",
      FILE = "file",
      G_FLAG = "g",
      FIND_CLI = "Find-CLI",
      DIRECTORY = "directory",
      EMPTY_STRING = "",
      ROOT_DIRECTORY_PATH = "./";

module.exports = {
  FIND,
  FILE,
  G_FLAG,
  FIND_CLI,
  DIRECTORY,
  EMPTY_STRING,
  ROOT_DIRECTORY_PATH
};
