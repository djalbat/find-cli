"use strict";

const FIND = "find",
      FILE = "file",
      FIND_CLI = "Find-CLI",
      DIRECTORY = "directory",
      CURRENT_DIRECTORY_PATH = ".";

module.exports = {
  FIND,
  FILE,
  FIND_CLI,
  DIRECTORY,
  CURRENT_DIRECTORY_PATH
};
