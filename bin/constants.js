"use strict";

const FIND = "find",
      FILE = "file",
      FIND_CLI = "Find-CLI",
      DIRECTORY = "directory",
      EMPTY_STRING = "",
      CURRENT_DIRECTORY_PATH = ".";

module.exports = {
  FIND,
  FILE,
  FIND_CLI,
  DIRECTORY,
  EMPTY_STRING,
  CURRENT_DIRECTORY_PATH
};
